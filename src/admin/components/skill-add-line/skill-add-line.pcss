  .skill-add-line__wrapper {
    display: flex;
  }

  .skill-add-line__wrapper.disabled {
    opacity: 0.7;
    filter: grayscale(70%);
    pointer-events: none;
  }

  .skill-add-line__name {
    margin-right: 8px;
  }

  .skill-add-line__percent {
    margin-right: 29px;
    width: 25%;
  }
